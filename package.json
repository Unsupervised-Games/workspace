{
  "name": "{{WORKSPACE_SLUG}}",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "check-types": "turbo check-types",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@10.11.0",
  "pnpm": {
    "overrides": {
      "@unsupervised/config": "workspace:*",
      "@unsupervised/forge": "workspace:*",
      "@unsupervised/packager": "workspace:*",
      "@unsupervised/schemas": "workspace:*"
    }
  }
}
