// Tauri's build script. Generates the `tauri_context!` runtime
// embedding from `tauri.conf.json` + capabilities/. Auto-synthesized
// by `@unsupervised/packager` — do not edit here.

fn main() {
    tauri_build::build()
}
