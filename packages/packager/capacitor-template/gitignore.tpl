# Packager-synthesized Capacitor cache directory. Re-generated on
# every `packager --target capacitor|ios|android` invocation. Don't
# commit anything here — including the native `ios/` and `android/`
# projects, which Capacitor regenerates via `cap add` and are
# deterministically reproducible from `capacitor.config.json` +
# `package.json`.
*
!.gitignore
